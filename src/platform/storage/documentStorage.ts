import { supabase } from '../supabase/client'
export interface StoredDocumentObject { bucket:string;objectPath:string;originalFileName:string;mimeType:string;size:number;uploadedAt:string;uploader:string;checksum?:string }
export interface DocumentStorage { upload(ownerId:string,documentId:string,file:File):Promise<StoredDocumentObject>;download(path:string):Promise<Blob>;remove(path:string):Promise<void> }
export class SupabaseDocumentStorage implements DocumentStorage {private bucket='compliance-documents';async upload(ownerId:string,documentId:string,file:File){if(!supabase)throw new Error('Supabase is not configured.');const objectPath=`${ownerId}/${documentId}/${crypto.randomUUID()}-${file.name}`;const {error}=await supabase.storage.from(this.bucket).upload(objectPath,file,{contentType:file.type,upsert:false});if(error)throw error;return{bucket:this.bucket,objectPath,originalFileName:file.name,mimeType:file.type,size:file.size,uploadedAt:new Date().toISOString(),uploader:ownerId}}async download(path:string){if(!supabase)throw new Error('Supabase is not configured.');const {data,error}=await supabase.storage.from(this.bucket).download(path);if(error)throw error;return data}async remove(path:string){if(!supabase)throw new Error('Supabase is not configured.');const {error}=await supabase.storage.from(this.bucket).remove([path]);if(error)throw error}}
