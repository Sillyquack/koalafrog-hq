import { useState } from 'react'
import type { ProductStage, ProductStatus } from '../../../types/domain'
import { useFormulaData } from '../../formulas/state/FormulaDataContext'
import { optionalDateValue } from '../domain/productDates'
export function ProductForm({onClose,prerequisite}:{onClose:()=>void;prerequisite?:string}){const data=useFormulaData();const[error,setError]=useState('');const submit=(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();const form=new FormData(event.currentTarget);try{data.createProduct({name:String(form.get('name')),category:String(form.get('category')),status:String(form.get('status')) as ProductStatus,developmentStage:String(form.get('stage')) as ProductStage,description:String(form.get('description')),scentProfile:String(form.get('scent')),targetLaunchDate:optionalDateValue(form.get('target'))});onClose()}catch(cause){setError(cause instanceof Error?cause.message:'Could not create Product.')}};return <div className="modal-backdrop"><form className="workspace-modal" onSubmit={submit}><span className="eyebrow">Portfolio / New record</span><h2>New Product</h2>{prerequisite&&<p className="modal-intro">{prerequisite}</p>}<div className="form-grid"><label>Name<input name="name" required autoFocus/></label><label>Category<input name="category" required/></label><label>Status<select name="status"><option>Active</option><option>On hold</option></select></label><label>Development stage<select name="stage"><option>Idea</option><option>Research</option><option>Formulation</option><option>Testing</option></select></label><label>Scent profile<input name="scent"/></label><label>Target launch<input name="target" type="date"/></label><label className="span-2">Description<textarea name="description"/></label></div>{error&&<p className="form-error">{error}</p>}<footer><button type="button" className="button ghost" onClick={onClose}>Cancel</button><button className="button primary">Create Product</button></footer></form></div>}
