import { useFormulaData } from '../../formulas/state/FormulaDataContext'
export function TestResponseForm({sessionId,onClose}:{sessionId:string;onClose:()=>void}){const data=useFormulaData();const session=data.testSessions.find(s=>s.id===sessionId)!;const template=data.testTemplates.find(t=>t.id===session.testTemplateId)!;const submit=(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);data.addTestResponse({testSessionId:sessionId,testerId:String(f.get('testerId')),answers:template.questions.map(q=>({questionId:q.id,value:q.type==='Numeric Rating'?Number(f.get(q.id)):String(f.get(q.id))})),overallNotes:String(f.get('overallNotes'))});onClose()};return <div className="modal-backdrop"><form className="workspace-modal" onSubmit={submit}><span className="eyebrow">Internal development feedback</span><h2>Add Test Response</h2><div className="form-grid"><label>Tester<select name="testerId">{data.testers.filter(t=>t.status==='Active').map(t=><option value={t.id} key={t.id}>{t.displayName}</option>)}</select></label>{template.questions.map(q=><label className={q.type==='Free Text'?'span-2':''} key={q.id}>{q.prompt}{q.type==='Numeric Rating'?<input name={q.id} type="number" min="1" max="5" required/>:<textarea name={q.id}/>}</label>)}<label className="span-2">Overall notes<textarea name="overallNotes"/></label></div><footer><button type="button" className="button ghost" onClick={onClose}>Cancel</button><button className="button primary">Submit immutable response</button></footer></form></div>}
