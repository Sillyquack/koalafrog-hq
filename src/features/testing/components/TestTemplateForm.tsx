import { useFormulaData } from '../../formulas/state/FormulaDataContext'
export function TestTemplateForm({onClose}:{onClose:()=>void}){const data=useFormulaData();const submit=(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();const form=new FormData(event.currentTarget);data.createTestTemplate(String(form.get('name')),String(form.get('questions')).split(',').map(q=>q.trim()).filter(Boolean));onClose()};return <div className="modal-backdrop"><form className="workspace-modal" onSubmit={submit}><span className="eyebrow">Reusable development evaluation</span><h2>Create Test Template</h2><div className="form-grid"><label className="span-2">Template name<input name="name" required/></label><label className="span-2">Numeric rating questions<textarea name="questions" defaultValue="Absorption, Greasiness, Skin feel, Scent strength, Overall impression" required/></label></div><p className="modal-intro">Initial custom questions use a 1–5 numeric rating. The domain supports additional focused question types.</p><footer><button type="button" className="button ghost" onClick={onClose}>Cancel</button><button className="button primary">Save template</button></footer></form></div>}
