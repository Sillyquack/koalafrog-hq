import { useFormulaData } from '../../formulas/state/FormulaDataContext'
export function TestSessionForm({batchId,onClose}:{batchId?:string;onClose:()=>void}){const data=useFormulaData();const submit=(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);data.createTestSession({labBatchId:String(f.get('batchId')),testTemplateId:String(f.get('templateId')),name:String(f.get('name')),status:'Active',dueDate:String(f.get('dueDate'))||undefined,notes:String(f.get('notes'))});onClose()};return <div className="modal-backdrop"><form className="workspace-modal" onSubmit={submit}><span className="eyebrow">Batch-linked evaluation</span><h2>Create Test Session</h2><div className="form-grid"><label>Lab batch<select name="batchId" defaultValue={batchId}>{data.labBatches.filter(b=>['In Progress','Completed'].includes(b.status)).map(b=><option value={b.id} key={b.id}>{b.batchNumber}</option>)}</select></label><label>Template<select name="templateId">{data.testTemplates.map(t=><option value={t.id} key={t.id}>{t.name}</option>)}</select></label><label className="span-2">Session name<input name="name" required/></label><label>Due date<input name="dueDate" type="date"/></label><label className="span-2">Notes<textarea name="notes"/></label></div><footer><button type="button" className="button ghost" onClick={onClose}>Cancel</button><button className="button primary">Create session</button></footer></form></div>}
