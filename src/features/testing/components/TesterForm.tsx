import { useFormulaData } from '../../formulas/state/FormulaDataContext'
export function TesterForm({onClose}:{onClose:()=>void}){const data=useFormulaData();const submit=(event:React.FormEvent<HTMLFormElement>)=>{event.preventDefault();const form=new FormData(event.currentTarget);data.createTester(String(form.get('name')),String(form.get('notes')));onClose()};return <div className="modal-backdrop"><form className="workspace-modal" onSubmit={submit}><span className="eyebrow">Lightweight evaluator identity</span><h2>Add Tester</h2><div className="form-grid"><label>Display name<input name="name" required/></label><label className="span-2">Notes<textarea name="notes"/></label></div><footer><button type="button" className="button ghost" onClick={onClose}>Cancel</button><button className="button primary">Save tester</button></footer></form></div>}
