import{describe,expect,it}from'vitest'
import type{Ingredient,SupplierProduct}from'../../../types/domain'
import{emptySupplierProductVerification,gradeSuggestions,inheritedSupplierProductProfile,supplierProductCompletion,supplierProductNeedsReview}from'./supplierProductIntelligence'
const ingredient={id:'i',commonName:'Powder',inciName:'Exact INCI',category:'Active',functions:[],cosingFunctions:['ABSORBENT'],description:'Research profile',defaultUnit:'mg',notes:'',status:'Research',referenceEntryId:'ref',createdAt:'',updatedAt:''} as Ingredient
describe('Supplier Product intelligence',()=>{it('inherits identity without mutating the Ingredient',()=>{const before=structuredClone(ingredient),profile=inheritedSupplierProductProfile(ingredient);expect(profile).toMatchObject({declaredInci:'Exact INCI',categorySnapshot:'Active',defaultInventoryUnit:'mg',cosingFunctionsSnapshot:['ABSORBENT'],researchProfileSnapshot:'Research profile',referenceEntryId:'ref'});expect(ingredient).toEqual(before)});it('preserves explicit product overrides',()=>expect(inheritedSupplierProductProfile(ingredient,{declaredInci:'Supplier declaration',defaultInventoryUnit:'g'} as SupplierProduct)).toMatchObject({declaredInci:'Supplier declaration',defaultInventoryUnit:'g'}));it('keeps verification operational',()=>{const verification=emptySupplierProductVerification();expect(supplierProductNeedsReview({verification} as SupplierProduct)).toBe(true);expect(Object.values(verification)).not.toContain('approved')});it('offers category-specific grades while keeping a custom input possible',()=>{expect(gradeSuggestions('Carrier oil')).toContain('High Oleic');expect(gradeSuggestions('Essential oil')).toContain('Steam Distilled');expect(gradeSuggestions('Butter')).toContain('Deodorised');expect(gradeSuggestions('Wax ester')).toContain('Yellow');expect(gradeSuggestions('Fragrance Oil')).toContain('Custom Blend');expect(gradeSuggestions('Botanical extract')).toContain('Glycerite')});it('treats completion as optional guidance',()=>expect(supplierProductCompletion({supplierName:'Supplier',productName:'Product',packageQuantity:100,packageUnit:'g'})).toMatchObject({core:100,verification:0,documentation:0}))})
