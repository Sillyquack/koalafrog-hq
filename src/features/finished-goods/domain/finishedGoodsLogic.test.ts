import { describe,expect,it } from 'vitest'
import type { FinishedGoodsBatch,FinishedGoodsMovement,ProductionRun } from '../../../types/domain'
import { finishedGoodsBalance,finishedGoodsCostBasis,generateFinishedGoodsNumber,remainingProductionOutput,validateFinishedGoodsMovement,validateFinishedGoodsOutput } from './finishedGoodsLogic'
const run={id:'pr',actualUnitsProduced:20} as ProductionRun
const batch:FinishedGoodsBatch={id:'fg',finishedGoodsBatchNumber:'KF-FG-BO-260714-01',productionRunId:'pr',productId:'p',formulaVersionId:'v',status:'Active',productionDate:'2026-07-14',initialQuantity:15,unit:'pcs',notes:'',createdAt:'',updatedAt:''}
const movement=(id:string,type:FinishedGoodsMovement['type'],quantity:number):FinishedGoodsMovement=>({id,finishedGoodsBatchId:'fg',type,quantity,unit:'pcs',reason:'',notes:'',occurredAt:'',createdAt:''})
describe('Finished Goods',()=>{it('generates a unique batch number',()=>expect(generateFinishedGoodsNumber('Beard Oil',['KF-FG-BO-260714-01'],new Date('2026-07-14'))).toBe('KF-FG-BO-260714-02'));it('tracks remaining Production output',()=>expect(remainingProductionOutput(run,[batch])).toBe(5));it('prevents registration above actual output',()=>expect(validateFinishedGoodsOutput(run,[batch],10)).toContain('exceeds'));it('derives ledger balance',()=>expect(finishedGoodsBalance(batch,[movement('r','ProductionReceipt',15),movement('s','Sample',2),movement('w','Waste',1)])).toBe(12));it('blocks Finished Goods over-consumption',()=>expect(validateFinishedGoodsMovement(batch,[movement('r','ProductionReceipt',5)],{type:'Sale',quantity:6,unit:'pcs'})).toContain('exceeds'));it('combines Production and physical packaging cost basis',()=>expect(finishedGoodsCostBasis(20,5,15,100)).toMatchObject({total:115,perUnit:23}));it('warns that physical packaging prevents double counting',()=>expect(finishedGoodsCostBasis(20,5,15,100).warning).toContain('authoritative'))})
