import{useState}from'react';import{Link,useParams}from'react-router-dom';import{ArrowLeft,Plus}from'lucide-react';import{useProcurement}from'./useProcurement';import{procurementActions}from'./actions/procurementActions';import{equipmentReadiness}from'./domain/procurement'
export function EquipmentDetailPage(){const{id=''}=useParams(),{workspace,data,error,refresh}=useProcurement(),[recording,setRecording]=useState(false),[message,setMessage]=useState('');if(error)return <section className="panel" role="alert">{error}</section>;if(!data)return <section className="panel">Loading Equipment…</section>;const item=data.equipment.find(x=>x.id===id);if(!item)return <section className="panel">Equipment unavailable.</section>;const capabilities=data.capabilities.filter(x=>x.equipment_item_id===id),events=data.serviceEvents.filter(x=>x.equipment_item_id===id),policy=data.equipmentPolicies.find(x=>x.equipment_item_id===id),readiness=equipmentReadiness(item,capabilities,policy,events);const record=async(form:HTMLFormElement)=>{if(!workspace)return;const values=Object.fromEntries(new FormData(form));try{await procurementActions.recordService(workspace.workspaceId,{equipment_item_id:id,event_type:values.type,performed_at:new Date().toISOString(),result_status:'recorded',notes:values.notes||''});setRecording(false);await refresh()}catch(cause){setMessage(cause instanceof Error?cause.message:'Could not record event.')}};return <div className="equipment-detail"><Link className="back-link" to="/equipment"><ArrowLeft size={14}/>Equipment</Link><header className="detail-hero"><span className="eyebrow">{item.equipment_type.replaceAll('_',' ')}</span><h1>{item.name}</h1><p>{readiness.label}</p><button className="button primary" onClick={()=>setRecording(x=>!x)}><Plus size={14}/>Record service event</button></header>{recording&&<form className="panel compact-create" onSubmit={e=>{e.preventDefault();void record(e.currentTarget)}}><label>Event<select name="type"><option value="cleaning">Cleaning</option><option value="calibration">Calibration</option><option value="maintenance">Maintenance</option><option value="verification">Verification</option><option value="repair">Repair</option></select></label><label>Notes<input name="notes"/></label><button className="button primary">Record append-only event</button>{message&&<p role="alert">{message}</p>}</form>}<div className="detail-columns"><section className="panel"><h2>Technical data</h2><dl><dt>Status</dt><dd>{item.status.replaceAll('_',' ')}</dd><dt>Capacity</dt><dd>{item.capacity_value==null?'Not recorded':`${item.capacity_value} ${item.capacity_unit}`}</dd><dt>Precision</dt><dd>{item.precision_value==null?'Not recorded':`${item.precision_value} ${item.precision_unit}`}</dd><dt>Location</dt><dd>{item.location||'Not recorded'}</dd></dl></section><section className="panel readiness-panel"><h2>Operational readiness</h2>{readiness.issues.length?readiness.issues.map(x=><p key={x}>{x}</p>):<p>No issue identified under recorded rules.</p>}<small>This is not safety, compliance or scientific certification.</small></section></div><section className="panel"><h2>Capabilities</h2>{capabilities.map(x=><p key={x.id}><strong>{x.capability_type.replaceAll('_',' ')}</strong> · {x.precision==null?'Precision not assessed':`precision ${x.precision} ${x.unit}`}</p>)}{!capabilities.length&&<p>No capabilities recorded.</p>}</section><section className="panel service-history"><h2>Append-only service history</h2>{events.map(x=><article key={x.id}><strong>{x.event_type}</strong><time>{new Date(x.performed_at).toLocaleString()}</time><p>{x.notes||'No notes.'}</p></article>)}{!events.length&&<p>No cleaning, calibration, maintenance or verification evidence recorded.</p>}</section></div>}
