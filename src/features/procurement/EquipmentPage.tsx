import{useState}from'react';import{Link}from'react-router-dom';import{Gauge,Plus,Search,Wrench}from'lucide-react';import{PageHeader}from'../../components/ui/PageHeader';import{useProcurement}from'./useProcurement';import{procurementActions}from'./actions/procurementActions';import{equipmentReadiness}from'./domain/procurement'
export function EquipmentPage(){const{workspace,data,error,refresh}=useProcurement(),[search,setSearch]=useState(''),[creating,setCreating]=useState(false),[message,setMessage]=useState('');if(error)return <section className="panel procurement-state" role="alert"><h1>Equipment unavailable</h1><p>{error}</p></section>;if(!data)return <section className="panel procurement-state">Loading Equipment…</section>;const create=async(form:HTMLFormElement)=>{if(!workspace)return;const values=Object.fromEntries(new FormData(form));try{await procurementActions.createEquipment(workspace.workspaceId,{name:values.name,equipment_type:values.type,status:'available',location:values.location||null,internal_notes:''});setCreating(false);await refresh()}catch(cause){setMessage(cause instanceof Error?cause.message:'Could not save Equipment.')}};const visible=data.equipment.filter(x=>!x.archived_at&&`${x.name} ${x.equipment_type}`.toLowerCase().includes(search.toLowerCase()));return <div className="equipment-page"><PageHeader eyebrow="Operational assets / evidence" title="Equipment" description="Capacity, capability and append-only service evidence. Readiness is operational guidance, never a safety certification." action={<button className="button primary" onClick={()=>setCreating(x=>!x)}><Plus size={14}/>Add Equipment</button>}/>{creating&&<form className="panel compact-create" onSubmit={e=>{e.preventDefault();void create(e.currentTarget)}}><label>Name<input name="name" required/></label><label>Type<select name="type"><option value="scale">Scale</option><option value="mixer">Mixer</option><option value="lab_vessel">Lab vessel</option><option value="production_vessel">Production vessel</option><option value="measurement">Measurement</option><option value="other">Other</option></select></label><label>Location<input name="location"/></label><button className="button primary">Save Equipment</button>{message&&<p role="alert">{message}</p>}</form>}<label className="procurement-search"><Search size={15}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search equipment"/></label><section className="equipment-library">{visible.length?visible.map(item=>{const ready=equipmentReadiness(item,data.capabilities,data.equipmentPolicies.find(x=>x.equipment_item_id===item.id),data.serviceEvents);return <Link className="panel" key={item.id} to={`/equipment/${item.id}`}><Gauge/><div><span className="eyebrow">{item.equipment_type.replaceAll('_',' ')}</span><h2>{item.name}</h2><p>{item.location||'Location unknown'} · {item.capacity_value==null?'Capacity unknown':`${item.capacity_value} ${item.capacity_unit}`}</p></div><strong className={ready.state}>{ready.state}</strong></Link>}):<article className="panel procurement-empty"><Wrench/><h2>No Equipment recorded</h2><p>Register owned equipment and add service evidence before relying on readiness checks.</p></article>}</section></div>}
