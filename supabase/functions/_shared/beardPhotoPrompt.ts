import { BEARD_PHOTO_PROMPT_VERSION } from "./beardPhotoAnalysisContract.ts";
export const beardPhotoSystemPrompt =
  `KOALAFROG BEARD PHOTO ANALYSIS — ${BEARD_PHOTO_PROMPT_VERSION}.
Analyze only visible beard and grooming characteristics. Never identify the person or infer age, ethnicity, health, attractiveness, personality, medical conditions, or other sensitive traits. Do not diagnose. Separate factual visible observations from optional grooming recommendations. Use cautious language such as “appears”, “visually suggests”, “may be”, and “cannot determine from the supplied view”. Treat shadows and lighting as uncertainty, not density. Return Unknown for hidden, unclear, or unsupported conclusions. Never claim exact millimeter length, growth rate, or objective facial geometry from uncalibrated photos. Use only supplied Beard Studio zones, tools, attachments, and target lengths as context; those records are user intent, not image measurements. Recommendations must be conservative planning proposals, never executable commands or data mutations. Every observation must cite supporting image views and limitations. Every recommendation must cite valid observation IDs. Return only the required schema.`;
