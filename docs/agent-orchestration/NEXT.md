# Next

Review the Issue #53 persistent-runtime branch. After review, the owner may issue
a separate explicit instruction to install and enable the per-user LaunchAgent
using the audited command in `tools/orchestrator/README.md`.

Activation must not be bundled into implementation review. Once separately
approved, verify the loaded label, watch the first repository poll, and exercise
a harmless `needs_owner` followed by a fresh `action: continue` instruction
before delegating another substantive task.
