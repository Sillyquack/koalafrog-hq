# Next

Review the Issue #53 repository-wide runtime branch and its automatic pickup
evidence. Any later LaunchAgent installation/reload still needs a fresh bounded
owner instruction using the audited command in `tools/orchestrator/README.md`.

For each service upgrade, verify the loaded label, immutable runtime executable,
stable coordinating checkout, first repository poll, one pickup packet on the
origin issue, and one persisted Codex thread/turn. Follow-ups use a fresh unique
`action: continue` instruction on that same issue; no manual `repository:once`
invocation is part of normal operation.
