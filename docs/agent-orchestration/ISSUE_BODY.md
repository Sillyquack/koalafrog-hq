# Build local Codex orchestration bridge

Implement the local orchestration bridge defined under `docs/agent-orchestration/`.

Start with `START_HERE.md` and complete the proof-of-life acceptance test before using the bridge for Material Intelligence Phase A.

Do not deploy, apply production migrations, modify production data, expose secrets, merge to default branch, or touch unrelated product-domain code.
