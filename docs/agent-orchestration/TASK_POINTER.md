# Task Pointer

Create and use a GitHub issue as the durable task record for building this orchestrator. The issue should point Codex to `docs/agent-orchestration/START_HERE.md` and require an isolated implementation branch/worktree.
