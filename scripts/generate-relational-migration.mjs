import fs from 'node:fs'
import ts from 'typescript'

const source = ts.createSourceFile('domain.ts', fs.readFileSync('src/types/domain.ts', 'utf8'), ts.ScriptTarget.Latest, true)
const interfaces = new Map()
for (const node of source.statements) if (ts.isInterfaceDeclaration(node)) interfaces.set(node.name.text, node)
const formulaState = interfaces.get('FormulaState')
if (!formulaState) throw new Error('FormulaState not found')

const tableByCollection = {
  products:'products',formulas:'formulas',formulaVersions:'formula_versions',formulaLines:'formula_lines',ingredients:'ingredients',supplierProducts:'supplier_products',inventoryLots:'inventory_lots',inventoryMovements:'inventory_movements',labBatches:'lab_batches',labBatchLines:'lab_batch_lines',labBatchAllocations:'lab_lot_allocations',processSteps:'lab_process_steps',labObservations:'lab_observations',testers:'testers',testTemplates:'test_templates',testSessions:'test_sessions',testResponses:'test_responses',productionRuns:'production_runs',productionRunLines:'production_run_lines',productionRunAllocations:'production_lot_allocations',productionProcessSteps:'production_process_steps',costLines:'cost_lines',packagingComponents:'packaging_components',packagingSupplierProducts:'packaging_supplier_products',packagingInventoryLots:'packaging_inventory_lots',packagingInventoryMovements:'packaging_inventory_movements',packagingSpecifications:'packaging_specifications',packagingSpecificationVersions:'packaging_specification_versions',packagingSpecificationLines:'packaging_specification_lines',packagingAllocations:'packaging_allocations',finishedGoodsBatches:'finished_goods_batches',finishedGoodsMovements:'finished_goods_movements',responsiblePersons:'responsible_persons',complianceDossiers:'compliance_dossiers',complianceDocuments:'compliance_documents',regulatorySources:'regulatory_sources',regulatoryReviews:'regulatory_reviews',pifSections:'pif_evidence_sections',cpsrRecords:'cpsr_records',labelArtworkVersions:'label_artwork_versions',labelReviewItems:'label_checklist_items',inciDrafts:'inci_declarations',claims:'claims',claimEvidence:'claim_evidence',cpnpRecords:'cpnp_records',readinessIssues:'readiness_issues',launchPlans:'launch_plans',launchMilestones:'launch_milestones',launchDecisions:'launch_decisions',safetyEffectRecords:'undesirable_effect_records'
}
const embedded = new Set(['TestTemplate.questions','TestResponse.answers','ComplianceDossier.compositionSnapshot','RegulatoryReview.sourceIds','PifSection.documentIds'])
const snake = value => value.replace(/[A-Z]/g, letter => `_${letter.toLowerCase()}`)
const memberName = member => member.name.getText(source).replaceAll("'", '')
const sqlType = type => {
  if (type.kind === ts.SyntaxKind.NumberKeyword) return 'numeric'
  if (type.kind === ts.SyntaxKind.BooleanKeyword) return 'boolean'
  if (type.kind === ts.SyntaxKind.StringKeyword) return 'text'
  if (ts.isArrayTypeNode(type)) return type.elementType.kind === ts.SyntaxKind.NumberKeyword ? 'numeric[]' : 'text[]'
  if (ts.isUnionTypeNode(type)) return 'text'
  return 'text'
}
const requiredChecks = new Set(['packageQuantity','price','openingQuantity','plannedBatchSize','plannedQuantity','quantityPerUnit','initialQuantity','size'])
const nonzeroChecks = new Set(['quantity'])
const fks = {
  formulas:{productId:'products'},formula_versions:{formulaId:'formulas',derivedFromVersionId:'formula_versions'},formula_lines:{formulaVersionId:'formula_versions',ingredientId:'ingredients'},products:{currentDevelopmentFormulaVersionId:'formula_versions',currentApprovedFormulaVersionId:'formula_versions'},supplier_products:{ingredientId:'ingredients'},inventory_lots:{ingredientId:'ingredients',supplierProductId:'supplier_products'},inventory_movements:{inventoryLotId:'inventory_lots'},lab_batches:{productId:'products',formulaId:'formulas',formulaVersionId:'formula_versions'},lab_batch_lines:{labBatchId:'lab_batches',formulaLineId:'formula_lines',ingredientId:'ingredients'},lab_lot_allocations:{labBatchLineId:'lab_batch_lines',inventoryLotId:'inventory_lots',inventoryMovementId:'inventory_movements'},lab_process_steps:{labBatchId:'lab_batches'},lab_observations:{labBatchId:'lab_batches'},test_sessions:{labBatchId:'lab_batches',testTemplateId:'test_templates'},test_responses:{testSessionId:'test_sessions',testerId:'testers'},production_runs:{productId:'products',formulaId:'formulas',formulaVersionId:'formula_versions'},production_run_lines:{productionRunId:'production_runs',formulaLineId:'formula_lines',ingredientId:'ingredients'},production_lot_allocations:{productionRunLineId:'production_run_lines',inventoryLotId:'inventory_lots',inventoryMovementId:'inventory_movements'},production_process_steps:{productionRunId:'production_runs'},packaging_supplier_products:{packagingComponentId:'packaging_components'},packaging_inventory_lots:{packagingComponentId:'packaging_components',packagingSupplierProductId:'packaging_supplier_products'},packaging_inventory_movements:{packagingInventoryLotId:'packaging_inventory_lots'},packaging_specifications:{productId:'products'},packaging_specification_versions:{packagingSpecificationId:'packaging_specifications',derivedFromVersionId:'packaging_specification_versions'},packaging_specification_lines:{packagingSpecificationVersionId:'packaging_specification_versions',packagingComponentId:'packaging_components'},packaging_allocations:{finishedGoodsBatchId:'finished_goods_batches',packagingSpecificationLineId:'packaging_specification_lines',packagingInventoryLotId:'packaging_inventory_lots',packagingInventoryMovementId:'packaging_inventory_movements'},finished_goods_batches:{productionRunId:'production_runs',productId:'products',formulaVersionId:'formula_versions',packagingSpecificationVersionId:'packaging_specification_versions'},finished_goods_movements:{finishedGoodsBatchId:'finished_goods_batches'},compliance_dossiers:{productId:'products',formulaVersionId:'formula_versions',packagingSpecificationVersionId:'packaging_specification_versions',labelArtworkVersionId:'label_artwork_versions',responsiblePersonId:'responsible_persons',derivedFromDossierId:'compliance_dossiers'},regulatory_reviews:{complianceDossierId:'compliance_dossiers'},pif_evidence_sections:{complianceDossierId:'compliance_dossiers'},cpsr_records:{complianceDossierId:'compliance_dossiers',credentialDocumentId:'compliance_documents',cpsrDocumentId:'compliance_documents',assessedFormulaVersionId:'formula_versions',assessedPackagingSpecificationVersionId:'packaging_specification_versions'},label_artwork_versions:{productId:'products',formulaVersionId:'formula_versions',packagingSpecificationVersionId:'packaging_specification_versions',artworkDocumentId:'compliance_documents'},label_checklist_items:{complianceDossierId:'compliance_dossiers'},inci_declarations:{complianceDossierId:'compliance_dossiers'},claims:{productId:'products',complianceDossierId:'compliance_dossiers'},claim_evidence:{claimId:'claims',documentId:'compliance_documents'},cpnp_records:{complianceDossierId:'compliance_dossiers',responsiblePersonId:'responsible_persons',confirmationDocumentId:'compliance_documents'},readiness_issues:{complianceDossierId:'compliance_dossiers'},launch_plans:{productId:'products',complianceDossierId:'compliance_dossiers'},launch_milestones:{launchPlanId:'launch_plans'},launch_decisions:{launchPlanId:'launch_plans',complianceDossierId:'compliance_dossiers'},undesirable_effect_records:{productId:'products',finishedGoodsBatchId:'finished_goods_batches'}
}
const uniqueField = {inventory_lots:'internalLotNumber',packaging_inventory_lots:'internalLotNumber',lab_batches:'batchNumber',production_runs:'productionRunNumber',finished_goods_batches:'finishedGoodsBatchNumber'}
const collections = formulaState.members.map(member => {
  const collection = memberName(member)
  const element = member.type.elementType.typeName.text
  return {collection,table:tableByCollection[collection],node:interfaces.get(element)}
})
if (collections.some(item => !item.table || !item.node)) throw new Error('Unmapped FormulaState collection')

let sql = `-- Generated from src/types/domain.ts by scripts/generate-relational-migration.mjs\nalter table public.workspaces add column if not exists lifecycle_state text not null default 'empty' check (lifecycle_state in ('empty','importing','reconciliation_required','active','failed'));\ngrant select,insert,update on public.workspaces to authenticated;\ngrant select,insert,update on public.migration_runs to authenticated;\ngrant select,insert,update,delete on public.document_objects to authenticated;\n\n`
const foreignKeys = []
for (const {table,node} of collections) {
  const columns = ['workspace_id uuid not null','owner_id uuid not null','id text not null']
  for (const member of node.members) {
    const name = memberName(member)
    if (name === 'id' || embedded.has(`${node.name.text}.${name}`)) continue
    const nullable = member.questionToken ? '' : ' not null'
    const check = requiredChecks.has(name) ? ` check (${snake(name)} > 0)` : nonzeroChecks.has(name) ? ` check (${snake(name)} <> 0)` : ''
    columns.push(`${snake(name)} ${sqlType(member.type)}${nullable}${check}`)
  }
  columns.push('primary key (workspace_id,id)')
  foreignKeys.push(`alter table public.${table} add foreign key (workspace_id) references public.workspaces(id) on delete cascade deferrable initially deferred;`)
  for (const [field,parent] of Object.entries(fks[table] ?? {})) foreignKeys.push(`alter table public.${table} add foreign key (workspace_id,${snake(field)}) references public.${parent}(workspace_id,id) deferrable initially deferred;`)
  if (uniqueField[table]) columns.push(`unique (workspace_id,${snake(uniqueField[table])})`)
  sql += `create table public.${table}(\n  ${columns.join(',\n  ')}\n);\n\n`
}
sql += `create table public.test_template_questions(workspace_id uuid not null,owner_id uuid not null,id text not null,test_template_id text not null,prompt text not null,type text not null,sort_order numeric not null,choices text[],primary key(workspace_id,id),foreign key(workspace_id,test_template_id) references public.test_templates(workspace_id,id) on delete cascade deferrable initially deferred);\n`
sql += `create table public.test_response_answers(workspace_id uuid not null,owner_id uuid not null,test_response_id text not null,question_id text not null,value jsonb not null,primary key(workspace_id,test_response_id,question_id),foreign key(workspace_id,test_response_id) references public.test_responses(workspace_id,id) on delete cascade deferrable initially deferred,foreign key(workspace_id,question_id) references public.test_template_questions(workspace_id,id) deferrable initially deferred);\n`
sql += `create table public.compliance_composition_snapshots(workspace_id uuid not null,owner_id uuid not null,compliance_dossier_id text not null,formula_line_id text not null,ingredient_id text not null,ingredient_name_snapshot text not null,inci_name_snapshot text not null,concentration numeric not null,primary key(workspace_id,compliance_dossier_id,formula_line_id),foreign key(workspace_id,compliance_dossier_id) references public.compliance_dossiers(workspace_id,id) on delete cascade deferrable initially deferred,foreign key(workspace_id,formula_line_id) references public.formula_lines(workspace_id,id) deferrable initially deferred,foreign key(workspace_id,ingredient_id) references public.ingredients(workspace_id,id) deferrable initially deferred);\n\n`
sql += `create table public.regulatory_review_sources(workspace_id uuid not null,owner_id uuid not null,regulatory_review_id text not null,regulatory_source_id text not null,primary key(workspace_id,regulatory_review_id,regulatory_source_id),foreign key(workspace_id,regulatory_review_id) references public.regulatory_reviews(workspace_id,id) on delete cascade deferrable initially deferred,foreign key(workspace_id,regulatory_source_id) references public.regulatory_sources(workspace_id,id) deferrable initially deferred);\n`
sql += `create table public.pif_section_documents(workspace_id uuid not null,owner_id uuid not null,pif_section_id text not null,document_id text not null,primary key(workspace_id,pif_section_id,document_id),foreign key(workspace_id,pif_section_id) references public.pif_evidence_sections(workspace_id,id) on delete cascade deferrable initially deferred,foreign key(workspace_id,document_id) references public.compliance_documents(workspace_id,id) deferrable initially deferred);\n\n`
sql += `${foreignKeys.join('\n')}\n\n`
const allTables = [...collections.map(x=>x.table),'test_template_questions','test_response_answers','compliance_composition_snapshots','regulatory_review_sources','pif_section_documents']
sql += `do $$ declare t text; begin foreach t in array array[${allTables.map(x=>`'${x}'`).join(',')}] loop execute format('alter table public.%I enable row level security',t);execute format('create policy owner_all on public.%I for all using (owner_id=auth.uid()) with check (owner_id=auth.uid())',t);execute format('revoke all on public.%I from anon',t);execute format('grant select,insert,update,delete on public.%I to authenticated',t);end loop;end $$;\n\n`
sql += `create or replace function public.import_v9_relational(payload jsonb) returns jsonb language plpgsql security invoker set search_path=public,pg_temp as $$ declare uid uuid:=auth.uid();wid uuid;run_id uuid:=gen_random_uuid();collection text;tbl text;row_data jsonb;counts jsonb:='{}';begin if uid is null then raise exception 'Authentication required';end if;select id into wid from workspaces where owner_id=uid for update;if wid is null then insert into workspaces(owner_id,name,lifecycle_state) values(uid,'Koalafrog HQ','importing') returning id into wid;elsif exists(select 1 from products where workspace_id=wid) then raise exception 'Remote workspace is not empty';else update workspaces set lifecycle_state='importing' where id=wid;end if;set constraints all deferred;`
for (const {collection,table} of collections) sql += `\ncollection:='${collection}';tbl:='${table}';for row_data in select value from jsonb_array_elements(coalesce(payload->collection,'[]')) loop row_data:=row_data||jsonb_build_object('workspace_id',wid,'owner_id',uid);execute format('insert into public.%I select (jsonb_populate_record(null::public.%I,$1)).*',tbl,tbl) using row_data;end loop;counts:=counts||jsonb_build_object(collection,jsonb_array_length(coalesce(payload->collection,'[]')));`
sql += `\ninsert into test_template_questions select wid,uid,q->>'id',t->>'id',q->>'prompt',q->>'type',(q->>'sort_order')::numeric,array(select jsonb_array_elements_text(coalesce(q->'choices','[]'))) from jsonb_array_elements(coalesce(payload->'testTemplates','[]')) t cross join lateral jsonb_array_elements(coalesce(t->'questions','[]')) q;`
sql += `\ninsert into test_response_answers select wid,uid,r->>'id',a->>'question_id',a->'value' from jsonb_array_elements(coalesce(payload->'testResponses','[]')) r cross join lateral jsonb_array_elements(coalesce(r->'answers','[]')) a;`
sql += `\ninsert into compliance_composition_snapshots select wid,uid,d->>'id',c->>'formula_line_id',c->>'ingredient_id',c->>'ingredient_name_snapshot',c->>'inci_name_snapshot',(c->>'concentration')::numeric from jsonb_array_elements(coalesce(payload->'complianceDossiers','[]')) d cross join lateral jsonb_array_elements(coalesce(d->'composition_snapshot','[]')) c;insert into regulatory_review_sources select wid,uid,r->>'id',jsonb_array_elements_text(coalesce(r->'source_ids','[]')) from jsonb_array_elements(coalesce(payload->'regulatoryReviews','[]')) r;insert into pif_section_documents select wid,uid,p->>'id',jsonb_array_elements_text(coalesce(p->'document_ids','[]')) from jsonb_array_elements(coalesce(payload->'pifSections','[]')) p;update workspaces set lifecycle_state='reconciliation_required',updated_at=now() where id=wid;insert into migration_runs(id,workspace_id,owner_id,source_version,state,stage,entity_counts,imported_counts,skipped_counts,warnings,errors,reconciliation,completed_at) values(run_id,wid,uid,'v9','Imported — Reconciliation Required','relational_import',counts,counts,'{}','[]','[]','{}',now());return jsonb_build_object('migrationRunId',run_id,'workspaceId',wid,'ownerId',uid,'counts',counts,'state','Imported — Reconciliation Required');exception when others then raise;end $$;revoke all on function public.import_v9_relational(jsonb) from public,anon;grant execute on function public.import_v9_relational(jsonb) to authenticated;\n`
sql += `create or replace function public.complete_v9_reconciliation(run_id uuid,report jsonb) returns void language plpgsql security invoker set search_path=public,pg_temp as $$ begin if auth.uid() is null or coalesce((report->>'complete')::boolean,false)=false then raise exception 'A complete authenticated reconciliation report is required';end if;update migration_runs set state='Completed',stage='reconciliation',reconciliation=report,completed_at=now() where id=run_id and owner_id=auth.uid();if not found then raise exception 'Migration run not found';end if;end $$;revoke all on function public.complete_v9_reconciliation(uuid,jsonb) from public,anon;grant execute on function public.complete_v9_reconciliation(uuid,jsonb) to authenticated;\n`
sql += `create or replace function public.record_v9_migration_failure(error_message text) returns uuid language plpgsql security invoker set search_path=public,pg_temp as $$ declare uid uuid:=auth.uid();wid uuid;run_id uuid:=gen_random_uuid();begin if uid is null then raise exception 'Authentication required';end if;select id into wid from workspaces where owner_id=uid;if wid is null then insert into workspaces(owner_id,name,lifecycle_state) values(uid,'Koalafrog HQ','failed') returning id into wid;else update workspaces set lifecycle_state='failed',updated_at=now() where id=wid;end if;insert into migration_runs(id,workspace_id,owner_id,source_version,state,stage,errors,completed_at) values(run_id,wid,uid,'v9','Failed','relational_import',jsonb_build_array(error_message),now());return run_id;end $$;revoke all on function public.record_v9_migration_failure(text) from public,anon;grant execute on function public.record_v9_migration_failure(text) to authenticated;\n`

fs.writeFileSync('supabase/migrations/20260715090000_relational_domain.sql', sql)
